<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BencanaApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="coloresourceProviderrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.BencanaApp" parent="Base.Theme.BencanaApp" />


    <style name="ButtonFilter" parent="Widget.AppCompat.Button">
        <item name="android:textAllCaps">false</item>
        <item name="android:textColor">@color/primary_foreground</item>
        <item name="android:textStyle">bold</item>
    </style>
</resources>